SELECT MIN(age) FROM users;
-- mostrar la edad minima de los usuarios MIN(age) de la tabla users(FRON users)

SELECT Max(age) FROM users;
-- mostrar la edad maxima de los usuarios MAX(age) de la tabla users(FRON users)

-- solo mostrara el dato que se pide