SELECT * FROM users
-- "SELECT" selecciona,  todas las columnas "*" de la tabla "users" (FROM users)"

SELECT name FROM users
-- "SELECT" selecciona, solo la columna "name" de la tabla "users" (FROM users)

SELECT users_id,name FROM users
-- "SELECT" selecciona, solo las columnas "users_id,name" de la tabla "users" (FROM users)