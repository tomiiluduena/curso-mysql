CREATE TABLE persons ( --se indica el nombre de la tabla nueva,nos debemos posisiconar en la vase de datos
	id /*indicamos el nombre de la columna*/ INT, /*indicamos las configuraciones*/
	name VARCHAR(100),
    age INT,
    email VARCHAR(50),
    created DATE
);

